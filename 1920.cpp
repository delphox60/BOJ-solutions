#include <iostream>

using namespace std

    int
    main()
{
    int N, M;
    cin >> N;
    int N_list[N];
    for (int i = 0; i < N; i++)
    {
        int x;
        cin >> x;
        N_list[i] = x;
    }
    cin >> M;
    int M_list[M];
    for (int i = 0; i < M; i++)
    {
        int x;
        cin >> x;
        M_list[i] = x;
    }
    
}